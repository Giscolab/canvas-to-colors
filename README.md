{
  "title": "Canvas to Colors",
  "image_url": "https://img.shields.io/badge/Canvas_to_Colors-Professional_Web_Studio-blue?style=for-the-badge",
  "description": "Canvas to Colors is a React + TypeScript web studio that converts raster or SVG images into paint-by-numbers outputs. The application provides image analysis, configurable segmentation/merging, multiple visualization modes (original, contours, numbered, colorized, compare), and export capabilities (PNG, JSON, SVG). It is designed as a client-side processing workflow with optional Supabase authentication/profile storage and SQL migrations for backend data governance.",
  "installation": "1. Clone the repository.\\n\\n```bash\\ngit clone <your-repo-url>\\ncd paint-by-numbers-generator\\n```\\n\\n2. Install dependencies (npm is used in the existing lockfile).\\n\\n```bash\\nnpm install\\n```\\n\\n3. Configure environment variables for Supabase (required for auth/profile features). Create a `.env` file in the project root with: \\n\\n```bash\\nVITE_SUPABASE_URL=https://<your-project-ref>.supabase.co\\nVITE_SUPABASE_PUBLISHABLE_KEY=<your-anon-publishable-key>\\n```\\n\\n4. (Optional) Set up Supabase schema by applying SQL migrations located in `supabase/migrations/` using the Supabase CLI or dashboard SQL editor.\\n\\n5. Start the development server.\\n\\n```bash\\nnpm run dev\\n```\\n\\nThe Vite configuration is set to run on port `8080` by default.",
  "usage": "Run the app locally: \\n\\n```bash\\nnpm run dev\\n```\\n\\nProduction build and preview: \\n\\n```bash\\nnpm run build\\nnpm run preview\\n```\\n\\nLint codebase: \\n\\n```bash\\nnpm run lint\\n```\\n\\nTypical end-user workflow in the UI:\\n1. Upload an image (PNG/JPG, or SVG which is rasterized automatically).\\n2. Click **Analyze image** to compute color analysis and recommendations.\\n3. Tune parameters (number of colors, minimum region size, smoothness, merge tolerance, artistic options).\\n4. Run processing to generate numbered/contour/colorized outputs.\\n5. Export final assets from the export bar as PNG, JSON, or SVG.\\n\\nManual QA scenario examples are documented in `docs/manual-tests.md`.",
  "features": "- Interactive studio layout with dedicated left/center/right panes and bottom export bar.\\n- Color analysis and recommendation flow prior to processing.\\n- Processing pipeline executed with workers and progress feedback.\\n- Support for raster and SVG input (SVG is rasterized for processing).\\n- Multiple viewing modes: original, contours, numbered, colorized, compare.\\n- Project context with settings, preferences, autosave, zoom/pan helpers, and profiling hooks.\\n- Export options: PNG image export, JSON data export, and SVG vector export.\\n- Optional Supabase integration for authentication and user profile management.\\n- Supabase SQL migrations with RLS policies for `image_jobs` and `profiles`.",
  "configuration": "Application/runtime configuration points:\\n\\n1. **Environment variables** (Vite):\\n   - `VITE_SUPABASE_URL`\\n   - `VITE_SUPABASE_PUBLISHABLE_KEY`\\n\\n2. **Core processing and UI constants** are centralized in `src/config/constants.ts` (file size limits, worker timeouts, cache size, zoom bounds, export filenames).\\n\\n3. **Vite server/build config** is defined in `vite.config.ts`:\\n   - dev server host `::`\\n   - dev server port `8080`\\n   - manual vendor chunk splitting\\n\\n4. **Tailwind theme tokens** are defined in `tailwind.config.ts` and `src/styles/theme-pro.css`.\\n\\n5. **Supabase local project settings** exist in `supabase/config.toml`; database changes are versioned in `supabase/migrations/*.sql`.\\n\\nIf you do not need backend features, the front-end image pipeline still runs locally, but auth/profile-related functionality requires valid Supabase credentials.",
  "contributing": "1. Fork the repository and create a feature branch.\\n\\n```bash\\ngit checkout -b feature/your-change\\n```\\n\\n2. Make changes with TypeScript + ESLint compliance and preserve existing architecture patterns (hooks/context/lib split).\\n\\n3. Run quality checks before committing:\\n\\n```bash\\nnpm run lint\\nnpm run build\\n```\\n\\n4. If your change impacts image processing behavior, run or extend scenarios in `docs/manual-tests.md`.\\n\\n5. Commit with clear messages and open a pull request describing: scope, rationale, test evidence, and any migration/config impacts.",
  "license": "MIT (as indicated by the project documentation and badges). If distributing the project publicly, keep or add an explicit `LICENSE` file for clarity."
}
